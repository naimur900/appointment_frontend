/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'media.istockphoto.com',
            'booking.eminencesoftware.com',
            'flowbite.s3.amazonaws.com'
        ],
    },
};

export default nextConfig;
